# Sling.js SSR Client Example

This is an example of Sling.js Server Side Rendering (SSR) capabilities on the client-side.

## Serve Application

To serve the application, issue the following command:

```
npm run devServer
```
